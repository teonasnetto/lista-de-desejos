server:
  port: 8888

#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info,prometheus

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/teonasnetto/config-repo.git
#        bootstrap: true
#        encrypt:
#          enabled: false
#  security:
#    user:
#      name: root
#      password: root